# Dependencies for Google Colab.
#
# Colab already ships a CUDA-matched build of torch — do NOT reinstall it.
# This file is the same as requirements.txt but with torch omitted.
#   pip install -r requirements-colab.txt

transformers==4.44.2
peft==0.12.0
datasets==2.21.0
accelerate==0.33.0
evaluate==0.4.2
jiwer==3.0.4
librosa==0.10.2
soundfile==0.12.1
huggingface_hub==0.24.6
PyYAML==6.0.2
